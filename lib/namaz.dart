import 'package:flutter/material.dart';

class Namaz extends StatelessWidget {
  const Namaz({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SizedBox(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [Text('data')],
        ),
      ),
    );
  }
}
